package Table;

public class NumbersWithOneXTable extends BaseTable {

    public NumbersWithOneXTable() {
        super();
        setHeader("Numbers with only 1 X");
        setColumnTitles(null);
        setRowZero(null);
    }
}
