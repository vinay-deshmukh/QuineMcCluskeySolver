package Code;

public class ToDo {

    //Current issue is this being overwritten when making new step


    // 1 Change group constructors to normal ArrayList - Maybe Not
    // 2 Make sure input is safe, doesn't work sometimes if only a single value is entered
}
